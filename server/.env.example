# This is a template for the server environment variables
# Duplicate this file as .env in the server folder and fill in the values
# DO NOT commit the actual .env file to version control!

SUPABASE_JWT_SECRET=your_supabase_jwt_secret_from_dashboard
PORT=3001
